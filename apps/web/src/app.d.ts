declare global {
	namespace App {
		// interface Error {}
		interface Locals {
			maxChapter: number;
		}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
